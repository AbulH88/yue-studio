const $ = (id) => document.getElementById(id);
const projects = [{name:'TEST', subtitle:'NEW MUSICAL WORLD', art:'art-castle', datasets:0, tracks:0, checkpoint:'None', status:'Ready'}];
let datasets = [];
let currentDataset = null;
let runPollTimer = null;
const settingFields={wsl_distribution:'settingDistro',wsl_python:'settingPython',trainer_path:'settingTrainer',yue2_model_path:'settingYue2',mert_path:'settingMert',vae_path:'settingVae',tokenizer_head_path:'settingHead',runs_root:'settingRunsRoot',minimum_free_gb:'settingDisk'};
async function api(path,options={}){const response=await fetch(path,options);const data=await response.json();if(!response.ok||data.error)throw new Error(data.error||`Request failed (${response.status})`);return data;}
function renderProjects(){
  $('projectGrid').innerHTML=projects.map((p,i)=>`<article class="project-card" data-project="${i}"><div class="project-art ${p.art}">${i===0?'♜':i===1?'☾':'♬'}</div><div class="project-card-body"><button class="project-menu" data-delete="${i}" title="Delete project">•••</button><h3>${p.name}</h3><em>${p.subtitle}</em><div class="card-rule"></div><div class="project-meta"><div><span>DATASETS</span>${p.datasets} datasets</div><div><span>LAST CHECKPOINT</span>${p.checkpoint}</div><div><span>TRACKS</span>${p.tracks} tracks</div><div><span>LAST ACTIVITY</span>Today, 14:32</div></div><b class="project-status ${p.status==='Training'?'training':''}">${p.status}</b></div></article>`).join('');
  document.querySelectorAll('.project-card').forEach(c=>c.addEventListener('click',()=>openProject(Number(c.dataset.project))));
  document.querySelectorAll('[data-delete]').forEach(b=>b.addEventListener('click',(event)=>{event.stopPropagation();const i=Number(b.dataset.delete);if(confirm(`Delete project "${projects[i].name}"? This will remove its project data but not shared models or ComfyUI files.`)){projects.splice(i,1);renderProjects();}}));
}
function formatDuration(seconds){if(seconds===null||seconds===undefined)return '—';const total=Math.round(seconds);return `${String(Math.floor(total/60)).padStart(2,'0')}:${String(total%60).padStart(2,'0')}`;}
function renderTracks(){
  const tracks=currentDataset?.tracks||[];
  $('trackCount').textContent=`${tracks.length} track${tracks.length===1?'':'s'}`;
  $('tracks').innerHTML=tracks.length?tracks.map((t,i)=>`<div class="track-row"><span>${String(i+1).padStart(2,'0')}</span><b>${t.name}</b><span class="format ${t.name.split('.').pop().toUpperCase()}">${t.name.split('.').pop().toUpperCase()}</span><span>${formatDuration(t.duration)}</span><span class="status-pill ${t.has_caption?'':'review'}">${t.has_caption?'Captioned':'Needs review'}</span><span class="mini-wave"></span><button class="play" title="Audio path: ${t.path}">▶</button></div>`).join(''):'<div class="empty-table">No audio files have been added to this project.</div>';
}
function renderDatasets(){
  const cards=$('datasetCards');
  if(!datasets.length){cards.innerHTML='<div class="empty-dataset"><strong>No datasets yet</strong><span>Create a dataset and add your MP3, WAV, FLAC, or M4A files.</span><button class="dark-gold" id="emptyAddDataset">＋ Create Dataset</button></div>';const empty=$('emptyAddDataset');if(empty)empty.addEventListener('click',createDataset);}
  else {cards.innerHTML=datasets.map((d,i)=>`<button class="dataset-card ${currentDataset===d?'selected':''}" data-dataset="${i}"><strong>${d.name}</strong><span>${d.track_count} tracks · ${d.path}</span></button>`).join('');document.querySelectorAll('[data-dataset]').forEach(b=>b.addEventListener('click',()=>{currentDataset=datasets[Number(b.dataset.dataset)];renderDatasets();renderTracks();}));}
  const select=$('datasetSelect');if(select){select.innerHTML=datasets.length?datasets.map((d,i)=>`<option value="${i}">${d.name} (${d.track_count} tracks)</option>`).join(''):'<option>No datasets available</option>';select.disabled=!datasets.length;const currentIndex=datasets.indexOf(currentDataset);if(currentIndex>=0)select.value=String(currentIndex);select.onchange=()=>{currentDataset=datasets[Number(select.value)]||null;renderDatasets();};}
  renderTracks();
}
async function loadDatasets(){const response=await fetch('/api/dataset');const data=await response.json();datasets=data.datasets||[];currentDataset=datasets[0]||null;projects[0].datasets=datasets.length;projects[0].tracks=datasets.reduce((sum,d)=>sum+d.track_count,0);renderDatasets();}
async function createDataset(){const name=prompt('Dataset name');if(!name)return;const path=prompt('Full audio folder path (for example: C:\\Users\\jimi\\Music\\Test Music\\Medival)');if(!path)return;try{const response=await fetch('/api/dataset',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,path})});const data=await response.json();if(!response.ok||data.error)throw new Error(data.error||'Dataset could not be created.');await loadDatasets();alert(`Dataset "${name}" added with ${data.dataset.track_count} audio files.`);}catch(error){alert(error.message);}}
async function openProject(i){ $('projectsScreen').classList.add('hidden');$('detailScreen').classList.remove('hidden');$('crumbName').textContent=projects[i].name;$('projectName').textContent=projects[i].name;await loadDatasets(); }
function showProjects(){ $('detailScreen').classList.add('hidden');$('settingsScreen').classList.add('hidden');$('projectsScreen').classList.remove('hidden');$('crumbName').textContent='Projects'; }
async function showSettings(){ $('projectsScreen').classList.add('hidden');$('detailScreen').classList.add('hidden');$('settingsScreen').classList.remove('hidden');$('crumbName').textContent='Settings';await loadTrainingSettings();}
document.querySelectorAll('.rail-item').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.rail-item').forEach(x=>x.classList.remove('active'));b.classList.add('active');if(b.dataset.screen==='settings')showSettings();else showProjects();}));
function showTab(name){
  document.querySelectorAll('.tab').forEach(x=>x.classList.toggle('active',x.dataset.tab===name));
  document.querySelectorAll('.tab-screen').forEach(x=>x.classList.add('hidden-tab'));
  const panel=$(name==='datasets'?'datasetsTab':name==='runs'?'runsTab':name==='checkpoints'?'checkpointsTab':name==='models'?'modelsTab':'generateTab');
  if(panel)panel.classList.remove('hidden-tab');
}
document.querySelectorAll('.tab').forEach(b=>b.addEventListener('click',()=>showTab(b.dataset.tab)));
document.querySelectorAll('[data-jump]').forEach(b=>b.addEventListener('click',()=>showTab(b.dataset.jump)));
document.querySelectorAll('.steps button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.steps button').forEach(x=>x.classList.remove('chosen'));b.classList.add('chosen');}));
document.querySelectorAll('.steps button').forEach(b=>b.addEventListener('click',()=>{const input=$('customSteps');if(input)input.value=b.dataset.steps;}));
$('newProject').addEventListener('click',()=>{const name=prompt('Project name');if(name){projects.push({name,subtitle:'A NEW MUSICAL WORLD',art:'art-castle',datasets:0,tracks:0,checkpoint:'None',status:'Ready'});renderProjects();}});
$('addDataset').addEventListener('click',createDataset);
if($('emptyAddDataset'))$('emptyAddDataset').addEventListener('click',createDataset);
if($('applySteps'))$('applySteps').addEventListener('click',()=>{const value=Number($('customSteps').value);if(!Number.isInteger(value)||value<1){alert('Training steps must be a whole number greater than 0.');return;}document.querySelectorAll('.steps button').forEach(x=>x.classList.remove('chosen'));alert(`Training steps set to ${value}.`);});
function settingsFromForm(){const value={};Object.entries(settingFields).forEach(([key,id])=>{value[key]=key==='minimum_free_gb'?Number($(id).value):$(id).value.trim();});return value;}
function fillSettings(value){Object.entries(settingFields).forEach(([key,id])=>{if($(id)&&value[key]!==undefined)$(id).value=value[key];});}
async function loadTrainingSettings(){try{fillSettings(await api('/api/training/settings'));}catch(error){showSettingsMessage(error.message,true);}}
function showSettingsMessage(message,error=false){const target=$('settingsMessage');target.textContent=message;target.classList.toggle('error',error);}
function renderPreflight(result){$('preflightResults').innerHTML=(result.checks||[]).map(check=>`<div class="check-result ${check.ok?'ok':'fail'}"><strong>${check.ok?'✓':'×'} ${check.name}</strong><small>${check.message}</small></div>`).join('');showSettingsMessage(result.ok?'Connection ready. YuE Studio can reach the trainer environment.':'Some setup checks need attention.',!result.ok);}
if($('autoDetect'))$('autoDetect').addEventListener('click',async()=>{try{showSettingsMessage('Looking for your YuE2 environment…');const settings=await api('/api/training/autodetect',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({wsl_distribution:$('settingDistro').value.trim()||'Ubuntu'})});fillSettings(settings);showSettingsMessage('Detected available local paths. Save or test them now.');}catch(error){showSettingsMessage(error.message,true);}});
if($('saveSettings'))$('saveSettings').addEventListener('click',async()=>{try{fillSettings(await api('/api/training/settings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(settingsFromForm())}));showSettingsMessage('Settings saved on this computer.');}catch(error){showSettingsMessage(error.message,true);}});
if($('testConnection'))$('testConnection').addEventListener('click',async()=>{try{showSettingsMessage('Saving settings and testing Ubuntu, CUDA, models, and disk space…');$('preflightResults').innerHTML='';const settings=await api('/api/training/settings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(settingsFromForm())});fillSettings(settings);const result=await api('/api/training/preflight',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(settings)});renderPreflight(result);}catch(error){showSettingsMessage(error.message,true);}});
function formatElapsed(seconds){if(seconds===undefined||seconds===null)return '—';const total=Math.round(seconds);return `${Math.floor(total/60)}m ${total%60}s`;}
function renderRun(run){const active=['queued','validating','staging','preparing','training','stopping'].includes(run.status);$('runTitle').textContent=run.run_id||'No training run yet';$('runStatus').textContent=(run.stage||run.status||'idle').replaceAll('_',' ');$('runStep').textContent=`${run.step||0} / ${run.steps||0}`;$('runLoss').textContent=run.loss===undefined?'—':Number(run.loss).toFixed(3);$('runElapsed').textContent=formatElapsed(run.elapsed_seconds);$('runVram').textContent=run.vram_gb===undefined?'—':`${run.vram_gb.toFixed(1)} GB`;$('runProgressBar').style.width=run.steps?`${Math.min(100,(run.step||0)/run.steps*100)}%`:'0%';$('runError').textContent=run.error||'';$('runLog').textContent=(run.logs&&run.logs.length)?run.logs.join('\n'):'Training logs will appear here.';$('runLog').scrollTop=$('runLog').scrollHeight;$('stopTraining').disabled=!active;$('runCheckpoints').textContent=run.checkpoints?.length?`Checkpoints: ${run.checkpoints.map(x=>x.name).join(', ')}`:(run.run_id?'No checkpoints saved yet.':'');if(!active&&runPollTimer){clearInterval(runPollTimer);runPollTimer=null;}}
async function pollRun(){try{renderRun(await api('/api/training/status'));}catch(error){$('runError').textContent=error.message;}}
function beginRunPolling(){if(runPollTimer)clearInterval(runPollTimer);pollRun();runPollTimer=setInterval(pollRun,2000);}
if($('startTraining'))$('startTraining').addEventListener('click',async()=>{const steps=Number($('customSteps').value);if(!Number.isInteger(steps)||steps<1){alert('Enter a valid number of training steps first.');return;}if(!currentDataset){alert('Choose a dataset first.');return;}const button=$('startTraining');button.disabled=true;button.textContent='Checking setup…';try{const run=await api('/api/training/start',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:`${projects[0].name}-${currentDataset.name}`,dataset_name:currentDataset.name,dataset_path:currentDataset.path,steps,rank:Number($('loraRank').value)})});showTab('runs');renderRun(run);beginRunPolling();}catch(error){alert(error.message);if(error.message.includes('Setup check failed'))showSettings();}finally{button.disabled=false;button.textContent='▶   Start Training';}});
if($('stopTraining'))$('stopTraining').addEventListener('click',async()=>{if(!confirm('Stop the active training run? Its manifest, logs, and saved checkpoints will be kept.'))return;try{renderRun(await api('/api/training/stop',{method:'POST',headers:{'Content-Type':'application/json'},body:'{}'}));beginRunPolling();}catch(error){$('runError').textContent=error.message;}});
pollRun();
renderProjects();
